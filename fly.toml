# Fly.io config (free allowances). Deploy: `fly launch --copy-config` then `fly deploy`.
app = "mcp-integration-explorer"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  [http_service.checks]
    [http_service.checks.health]
      grace_period = "10s"
      interval = "30s"
      timeout = "5s"
      path = "/api/health"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
